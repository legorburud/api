cosas de las que dudo 
db: Session = Depends(get_db)  = crea la sesión a la base de datos, se usa la app y luego cierra la sesion 

#comando para poder usar el entorno virtual en vscode
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

